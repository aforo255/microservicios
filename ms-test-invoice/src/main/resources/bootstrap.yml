spring:
  application:
    name: ms-test-invoice
  profiles:
    active: dev
  cloud:
    config:
      #uri: http://localhost:8888
      uri: http://app-config-server:8888

management:
  endpoints:
    web:
      exposure:
        include: '*'


